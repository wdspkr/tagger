<%= form_with(model: tagged, local: true) do |form| %>
  <% if tagged.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tagged.errors.count, "error") %> prohibited this tagged from being saved:</h2>

      <ul>
      <% tagged.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :tagged_title %>
  </div>

  <div class="field">
    <%= form.label :creator %>
    <%= form.text_field :creator, id: :tagged_creator %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :tagged_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
